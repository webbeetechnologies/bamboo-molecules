import { OptionFlatList, OptionFlatListProps } from '../../../src/components';

export type Props = OptionFlatListProps & {};

export const Example = (props: Props) => {
    return <OptionFlatList {...props} />;
};
