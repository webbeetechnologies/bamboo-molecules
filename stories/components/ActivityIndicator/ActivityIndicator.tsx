import { ActivityIndicator, ActivityIndicatorProps } from '../../../src/components';

export type Props = ActivityIndicatorProps & {};

export const Example = (props: Props) => {
    return <ActivityIndicator {...props} />;
};
