import { useMolecules } from '../../src';

export const Example = () => {
    const { View, Text } = useMolecules();

    return (
        <View>
            <Text>Documentation Pending..</Text>
        </View>
    );
};
