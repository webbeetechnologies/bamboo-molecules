import type { ViewStyle } from 'react-native';
import type { ComponentStylePropWithVariants } from '../../types';

type CustomProps = {};

export const selectStyles: ComponentStylePropWithVariants<ViewStyle, '', CustomProps> = {};
